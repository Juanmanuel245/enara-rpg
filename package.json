{
  "name": "juego",
  "private": true,
  "scripts": {
    "dev": "node scripts/print-lan-url.mjs && concurrently -k -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
